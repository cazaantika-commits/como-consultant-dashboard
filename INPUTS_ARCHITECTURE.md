# هيكل الإدخالات — المرجع الدائم

> هذا الملف يُقرأ في بداية كل جلسة. لا يُحذف ولا يُعدل إلا بطلب صريح من المستخدم.

---

## القاعدة الذهبية

**كل رقم يُدخل في مكان واحد فقط. باقي الصفحات تقرأ ولا تكتب.**

---

## الهيكل: 4 صفحات إدخال + صفحة إعدادات

### 1. صفحة وائل (المبيعات والتسويق)

وائل مسؤول عن:
- التسعير (أنواع الوحدات، عدد كل نوع، مساحاتها، سعر القدم لكل نوع)
- خطة المبيعات (منحنى المبيعات — التوزيع الشهري لعدد الوحدات المباعة)
- نسبة التسويق والعمولة
- خطة الدفع للمشترين (الأقساط: حجز، إنشاء، تسليم)
- مواد الدعاية (المحتوى التسويقي، الحملات، القنوات، توزيع الميزانية)
- توزيع القنوات التسويقية (رقمي، خارجي، معارض، وسطاء، علاقات عامة، محتوى)

### 2. صفحة الإنشاء (المقاول)

مسؤول عنها: مدير المشروع / المهندس
- نسب الإنجاز الشهرية (S-Curve — مثلاً: شهر 1 = 5%، شهر 2 = 8%...)
- المبالغ المستحقة للمقاول مقابل كل نسبة إنجاز (مستخلصات)
- الدفعة المقدمة للمقاول (Mobilization)

### 3. صفحة الإدخالات العامة (كل شيء آخر)

كل رقم لا يخص وائل ولا الإنشاء يدخل هنا:
- بيانات المشروع الأساسية: مساحة الأرض، BUA، GFA (سكني/تجزئة/مكاتب)
- المدد: تصاميم، إنشاء، تسليم
- تاريخ البدء
- سعر الأرض
- تكلفة الإنشاء (درهم/قدم)
- النسب القابلة للبيع (سكني 95%، تجزئة 97%، مكاتب 95%)
- الرسوم الثابتة: فحص تربة، مسح طبوغرافي، مساح، مجتمع، جهات حكومية، فرز، NOC، ريرا (تسجيل + وحدات + ضمان + بنك + مدقق + فحص)
- النسب: تسجيل أرض 4%، وسيط 1%، تصميم 1.8%، إشراف 2%، فرز 40 د/قدم، مطور 5%، طوارئ 2%
- تقسيم التمويل: أي بند من المستثمر وأي بند من الضمان

### 4. صفحة الإعدادات (القواعد الزمنية والربط)

القرارات التشغيلية — "كيف ومتى" تُطبق الأرقام:
- "الحملة الإعلانية تبدأ بعد X شهر من انتهاء المخططات السكيماتيك"
- "المبيعات تبدأ قبل X شهر من نهاية التصاميم"
- "دفعات الرسوم الحكومية في الشهر X"
- "توزيع دفعات التصميم: X% عند التوقيع، Y% عند السكيماتيك، Z% عند الـ DD"
- "نسبة الإنشاء من المستثمر X% ومن الضمان Y%"
- أي قاعدة ربط زمنية أو نسبية بين الأحداث

---

## مصادر البيانات الحالية (DB)

| الجدول | الغرض | الحالة |
|--------|-------|--------|
| `projects` | البيانات الأساسية + التسعير + النسب + المدد | ✅ المصدر الرئيسي |
| `wael_sales_plans` | خطة وائل (منحنى + قنوات + دفعات) | ✅ خاص بوائل |
| `cf_projects` | مدد + مبيعات + خطة دفع | ⚠️ يكرر projects — يجب توحيد |
| `cf_project_costs` | بنود التكاليف مع نوع الدفع والمرحلة | ✅ تفصيل التكاليف |

---

## صفحات المخرجات (تقرأ فقط — لا تكتب)

هذه الصفحات تأخذ بياناتها من صفحات الإدخال الأربع:
- V2Feasibility — الجدوى المالية (إيرادات - تكاليف = ربح)
- V2InvestorCashFlow — تدفقات المستثمر الشهرية
- V2EscrowCashFlow — تدفقات حساب الضمان
- V2Portfolio — المحفظة المجمعة
- V2Timeline — الجدول الزمني

---

## المشكلة الحالية

صفحات V2 (Feasibility, InvestorCashFlow, EscrowCashFlow, WaelSales) تستخدم أرقام hardcoded ولا تقرأ من DB. يجب ربطها بمصدر واحد.
