# تدقيق التنظيف الآمن للتكرار والكود غير المستخدم

**الحالة:** قيد التدقيق والتنظيف الآمن. لا يحذف هذا المسار أي صفحة أو مصدر مشترك ما لم يثبت غياب كل مسار واستيراد واعتماد مضمّن.

## الدفعة الأولى المحذوفة بأمان

حُذفت اثنتا عشرة صفحة أو وحدة عميلة لم يكن لها أي مسار في التطبيق، أو استيراد، أو استخدام مضمّن، أو استدعاء ديناميكي. وشملت صفحات العرض أو المحاكاة القديمة، وواجهة جدوى بديلة، ووحدات مراحل تابعة لها، ومصدّر HTML مستقل لم يكن له أي استدعاء.

| الفئة | الوحدات المحذوفة | سبب الأمان |
|---|---|---|
| صفحات قديمة غير مسجلة | `ComponentShowcase`، `ConsultantProposalsPage`، `DashboardPage`، `DevelopmentStagesSimplified`، `FeasibilityStudySimplified` | لا يوجد مسار أو استيراد أو استخدام في الواجهة |
| مسار جدوى مهجور | `FeasibilityHubPage`، `FinancialFeasibilityTab`، `TimeDistributionTab`، `FlexiblePhasesTab` | الصفحة الأم غير مسجلة، ثم ثبت أن الوحدات التابعة لا يستهلكها أي مسار حي |
| نماذج مالية منفصلة غير مستخدمة | `CapitalSchedulingHorizontal`، `TrueCostReportPDF`، `projectCashFlowReport` | لا يوجد استيراد أو استدعاء من مسار حي؛ بقيت الشاشات والتقارير الفعالة البديلة |

## ما ثبت أنه يجب أن يبقى

صفحات الدراسات والتخطيط المالي غير المسجلة كمسارات مستقلة ليست ميتة؛ فهي مكوّنات مضمّنة ومحمّلة كسولاً داخل `BateekhaPage`، ومنها بطاقة المشروع، المدخلات العامة، الإنشاء، الإعدادات، الخط الزمني، تقارير المحفظة، وسيولة الإسكرو. كذلك تبقى محركات `investorCashFlowEngine` و`cashFlowEngine` و`projectCostsCalc` لأنها مستوردة من شاشات أو اختبارات أو مسارات حية، ولا تمثل مجرد تكرار اسم.

تبقى أيضاً مسارات URL البديلة مثل `/feasibility-study` و`/projects/:id` لأنها لا تضيف صفحة أو حساباً مستقلاً؛ إنها أسماء وصول متوافقة لنفس المكوّن. لن تحذف قبل مراجعة أثر الروابط المحفوظة أو الخارجية.

## الدفعة الثانية وخلاصة التحقق

حُذفت أيضاً وحدة REST قديمة غير مسجلة في خادم Express وتبلغ 3,745 سطراً، ووحدة تنفيذ مهام اجتماعات غير مستوردة من أي تدفق نشط. لا يؤثر الحذف في tRPC أو عمليات الأتمتة المسجلة في الخادم، لأن كلا الملفين لم يكن لهما أي استيراد أو تسجيل عند الإقلاع.

أظهر التحقق أن `projectCashFlowReport` كان يبدو غير مستورد في الفحص الأول بسبب صيغة المسار المختصرة، لكنه يُستدعى من صفحة تدفق الإسكرو ذات المسار الحي لتصدير HTML. لذلك لم يُحذف؛ خُفّض إلى طبقة توافق صغيرة تبقي زر التصدير يعمل من دون الاحتفاظ بقالب تقرير موازٍ ضخم. هذه نتيجة مهمة: الفحص لم يكتف باسم الملف، بل أعاد فتح الصفحة الحية بعد الحذف واكتشف المرجع وأعاده قبل أي تسليم.

أعيد فتح الدراسات والتخطيط المالي ومركز القيادة بعد التنظيف. ظهرت بطاقات الدراسات ومسارات التقارير الحية، كما ظهرت بطاقة الالتزامات، إنذار الإسكرو، وجلسات DATUM وARTEC في مركز القيادة. نجحت حزمة الانحدار بعد الحذف: **113/113**.
